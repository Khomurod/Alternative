# Page meta (from capture)

## Source

- File: `message.txt` (repo root)
- Generated lines: 852

## Opening `<html>` (truncated)

```html
<html lang="en" hydrated="" data-dat-ext-toolbar-mode="compact" data-dat-ext-summary-density="compact" style="--dat-ext-toolbar-max-width: 360px;"><head><style>body {transition: opacity ease-in 0.2s; } 
...
```

## Document title

- **Search Loads - DAT One**

## Extension state on `<html>`

From the opening tag, this capture includes DAT Dispatcher Assist **data attributes** (toolbar layout), e.g. `data-dat-ext-toolbar-mode`, `data-dat-ext-summary-density`, and CSS vars like `--dat-ext-toolbar-max-width`. That proves the extension was active when the snapshot was taken.
