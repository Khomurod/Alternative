# Inline CSS blob (line 192)

The original `message.txt` contains a **very long line 192** (~1,727,643 chars) of concatenated CSS (starts with `#menufication-top`). It is **not** DAT semantic markup; it bloats captures and is low priority for selector learning.

## Split files

- `02-inline-css-line192-HEAD.txt` — first ~120,000 chars
- `02-inline-css-line192-TAIL.txt` — last ~80,000 chars

For the untouched full line, open `message.txt` and go to **line 192**.
