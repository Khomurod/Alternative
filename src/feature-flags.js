/** Chrome storage key: assist column between Trip and Rate (default on; set false to disable). */
export const DAT_EXT_NUMEO_COLUMN_KEY = "datExtNumeoColumn";

export function defaultNumeoColumnEnabled() {
  return true;
}
